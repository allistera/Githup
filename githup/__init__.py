"""Githup: fan out Claude Agent SDK workers across your GitHub repos.

Each worker clones a repo, installs dependencies, updates them, resolves
Dependabot alerts, and opens a pull request with the result.
"""

__version__ = "0.1.0"
